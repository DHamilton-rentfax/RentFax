import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function RenterTable() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Renters</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Renter table will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
