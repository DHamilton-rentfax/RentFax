import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function DisputeTable() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Disputes</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Dispute table will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
