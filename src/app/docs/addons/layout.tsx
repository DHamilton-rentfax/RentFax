''''// This layout component provides a consistent structure for all add-on feature pages.
export default function AddonsLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
''''