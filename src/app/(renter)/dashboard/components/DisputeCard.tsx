'use client'\nimport { useState } from 'react'\n\nexport default function DisputeCard({ reportId }: { reportId: string }) {\n  const [message, setMessage] = useState('')\n  const [submitted, setSubmitted] = useState(false)\n\n  const handleSubmit = async () => {\n    const res = await fetch('/api/disputes', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ reportId, message }),\n    })\n    if (res.ok) setSubmitted(true)\n  }\n\n  if (submitted)\n    return (\n      <p className="text-sm text-emerald-700 mt-2">\n        ✅ Dispute submitted for review.\n      </p>\n    )\n\n  return (\n    <div className="mt-3">\n      <textarea\n        value={message}\n        onChange={(e) => setMessage(e.target.value)}\n        placeholder="Describe any errors or provide context..."\n        className="w-full border rounded-md p-2 text-sm resize-none"\n      />\n      <button\n        onClick={handleSubmit}\n        className="mt-2 text-sm text-white bg-emerald-600 hover:bg-emerald-700 rounded-md px-3 py-1"\n      >\n        Submit Dispute\n      </button>\n    </div>\n  )\n}\n