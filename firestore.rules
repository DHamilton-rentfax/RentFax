rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ========================
    // Users collection
    // ========================
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      // Super Admins can read/write anyone's user doc
      allow read, write: if isSuperAdmin();
    }

    // ========================
    // Dashboard data (per user/team)
    // ========================
    match /dashboard/{docId} {
      // Only logged-in users can read/write their own dashboard data
      allow read, write: if request.auth != null;
    }

    // ========================
    // Incidents collection
    // ========================
    match /incidents/{incidentId} {
      allow read: if request.auth != null; // renters/admins can read
      allow write: if isAdminOrSuper();    // only admin/super can create/update
    }

    // ========================
    // Disputes collection
    // ========================
    match /disputes/{disputeId} {
      allow read: if request.auth != null; // renter can see their disputes
      allow write: if request.auth != null; // renter can file disputes
    }

    // ========================
    // Admin-only area
    // ========================
    match /admin/{docId} {
      allow read, write: if isSuperAdmin();
    }
    
    // ========================
    // Notifications collection per user
    // ========================
    match /users/{userId}/notifications/{notificationId} {
      allow read, update, delete: if request.auth.uid == userId;
      allow create: if request.auth.uid == userId;
    }

    // ========================
    // Functions (role helpers)
    // ========================
    function isSuperAdmin() {
      return request.auth != null
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "SUPER_ADMIN";
    }

    function isAdminOrSuper() {
      return request.auth != null
        && (
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "ADMIN"
          || get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "SUPER_ADMIN"
        );
    }
  }
}