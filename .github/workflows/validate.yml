name: 🧩 RentFAX Validation Pipeline

on:
  push:
    branches: [main, production]
  pull_request:
    branches: [main, production]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Lint Firebase imports & syntax
        run: npm run lint

      - name: 🧠 Type check
        run: npm run type-check

      - name: 🏗️ Build test (Next.js)
        run: npm run build:test

      - name: ✅ Success
        run: echo "✔️ All checks passed — ready for deploy"
